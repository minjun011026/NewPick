<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.unit_3.sogong_test" >

    <!-- 필요한 퍼미션들 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sogong_test"
        android:usesCleartextTraffic="true"
        tools:targetApi="31" >

        <!-- 네이버 지도 클라이언트 ID 설정 -->
        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="cxo5tj8vfn" />

        <!-- 각 활동들 설정 -->
        <activity
            android:name=".SplashActivity"
            android:exported="true" > <!-- 스플래시 화면으로 설정 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true" /> <!-- 기본 메인 화면 -->

        <activity
            android:name=".MapViewActivity"
            android:exported="true" />

        <activity
            android:name=".WebViewActivity"
            android:exported="false" />

        <activity
            android:name=".TrendKeywordActivity"
            android:exported="false" />

        <activity
            android:name=".KeywordNewsActivity"
            android:exported="false" />

        <activity
            android:name=".FindPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name" />

        <activity
            android:name=".BookmarkedNewsActivity"
            android:exported="true" />

        <activity
            android:name=".SignUpActivity"
            android:exported="false" />

        <activity
            android:name=".ChangeNicknameActivity" />

        <activity
            android:name=".ChangeEmailActivity" />

        <activity
            android:name=".ChangePasswordActivity" />

    </application>

</manifest>





